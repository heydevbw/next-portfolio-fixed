# Lenscraft Portfolio (Next.js)

## 🚀 Quick Start

```bash
npm install
npm run dev
```

- Add images to `public/images/`  
- Add videos to `public/videos/`  
- Run `npm run build` to generate gallery and build production bundle

Deploy easily to **Vercel**, **Netlify**, or any static host.
